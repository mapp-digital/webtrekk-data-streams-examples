[DEFAULT]
consumer-group = <consumer-group>
endpoints = <host1:port[,host2:port]>
username = <username>
password = <password>
topic = <my-topic>

client-id = webtrekkDataStreamsDemo

auto-offset-reset-policy = latest

security-protocol = SASL_SSL
security-sasl-mechanism = SCRAM-SHA-256